[package]
name = "nex-executor"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "NexChain parallel transaction executor: conflict scheduling over declared access sets"

[dependencies]
nex-types = { workspace = true }
nex-state = { workspace = true }
