[package]
name = "phi-state"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Phi state machine: SMT-committed account store and deterministic state transition"

[dependencies]
phi-types = { workspace = true }
phi-crypto = { workspace = true }
